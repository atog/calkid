div#message

- if (exercises.length)
  - var count = 0
  ul
    - exercises.forEach(function(ex){
      li.h= cycle(ex, count++);
    - })
  form#exercise
    input(type='number', id='solution', size='10')
    input(type='submit', id='next', value='volgende')

script
  nextExercise();